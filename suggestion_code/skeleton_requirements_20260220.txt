# skeleton: requirements.txt
# /home/limonene/ROCm-project/MAGI-agents/requirements.txt
#
# 注意: これはスケルトンコードです。
# Codexは以下のコマンドでセットアップしてください:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt
#
# 変更履歴:
#   20260220 v1: 初版
#   20260220 v2: pydantic>=2.0 を追加（Structured Outputs対応）

requests>=2.31.0       # HTTP通信（タイムアウト明示必須・v3でhttpxに移行検討）
python-dotenv>=1.0.0   # .env読み込み
pyyaml>=6.0            # config.yaml読み込み
pydantic>=2.0          # LLMレスポンスのバリデーション（V2・Rust実装で高速）
